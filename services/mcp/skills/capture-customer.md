# Skill: capture-customer

Trigger when a user expresses interest, signs up, or provides contact details.

1. Extract email, name, phone from context.
2. Call `capture_customer` tool with extracted fields and `source` set to the interaction type.
3. Confirm capture to the user in formal, professional language.
